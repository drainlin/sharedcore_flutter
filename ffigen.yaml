# Run with `dart run ffigen --config ffigen.yaml`.
name: SharedcoreFlutterBindings
description: |
  Bindings for `src/sharedcore_flutter.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/sharedcore_flutter_bindings_generated.dart'
headers:
  entry-points:
    - 'src/sharedcore_flutter.h'
  include-directives:
    - 'src/sharedcore_flutter.h'
comments:
  style: any
  length: full
